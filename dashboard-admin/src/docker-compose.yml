version: "3.8"
services:
  dashboard-admin:
    build:
      context: .
      dockerfile: Dockerfile
    image: dashboard-admin:latest
    ports:
      - "8080:80"
    networks:
      - mynetwork

networks:
  mynetwork: